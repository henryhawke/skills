interface:
  display_name: "Opium"
  short_description: "Deep evidence-backed codebase completion audit"
  default_prompt: "Use $opium to run a read-only completion audit, inspect the latest ten provable checks, keep local, candidate, deployed, and operational proof separate, and return the exact evidence-backed critical path."
